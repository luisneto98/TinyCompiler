{ Sample program
  in TINY language -
  computes factorial
}
read _x; { input an integer }

while (0 < x) { don't compute if x <= 0 }
  
  fact := 1;
  repeat
    fact := fact * x;
    x := x - 1
  until x = 0;
  write fact  { output factorial of x }
endwhile
